import { SystemState } from '@/components/system/SystemState';

export default function Loading() {
  return <SystemState loading eyebrow="Sincronizando" title="Preparando Project Hub" description="Estamos organizando tus espacios, proyectos y equipos." />;
}
